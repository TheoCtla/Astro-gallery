<style>
  .about-cta-banner-section {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 1.5rem 2rem;
  }

  .about-cta-banner {
    padding: 2rem 1.5rem;
    background-color: #1D1D1B;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    border-radius: 12px;
    overflow: hidden;
    position: relative;
  }

  .about-cta-banner__container {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    text-align: center;
    position: relative;
    z-index: 1;
  }

  .about-cta-banner__heading {
    font-family: 'Lemon Milk', sans-serif;
    font-weight: 400;
    font-size: 13px;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: #FFFFFF;
    line-height: 1.7;
    margin: 0;
  }

  .about-cta-banner__right {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.2rem;
  }

  .about-cta-banner__text {
    font-family: 'Libre Baskerville', serif;
    font-style: italic;
    font-size: 13px;
    color: #DFBE71;
    line-height: 1.6;
    margin: 0;
  }

  .about-cta-banner__btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background-color: #DFBE71;
    color: #1D1D1B;
    border: none;
    padding: 0.8rem 2rem;
    text-decoration: none;
    font-family: 'Lemon Milk', sans-serif;
    font-weight: 400;
    font-size: 9px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
    white-space: nowrap;
  }

  .about-cta-banner__btn-text {
    position: relative;
    z-index: 1;
    padding: 0 0.5rem;
  }

  .about-cta-banner__btn::before,
  .about-cta-banner__btn::after {
    content: '';
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 4px;
    height: 4px;
    background-color: #1D1D1B;
    border-radius: 50%;
    z-index: 2;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .about-cta-banner__btn::before {
    left: 0.8rem;
  }

  .about-cta-banner__btn::after {
    right: 0.8rem;
  }

  @media (hover: hover) and (pointer: fine) {
    .about-cta-banner__btn:hover {
      padding-right: 2.5rem;
    }

    .about-cta-banner__btn:hover::after {
      content: '\2192';
      width: auto;
      height: auto;
      background: none;
      border-radius: 0;
      font-size: 14px;
      line-height: 1;
      color: #1D1D1B;
    }
  }

  /* Tablet */
  @media (min-width: 750px) {
    .about-cta-banner-section {
      padding: 0 2rem 2.5rem;
    }

    .about-cta-banner {
      padding: 3rem 3rem;
    }

    .about-cta-banner__container {
      flex-direction: row;
      align-items: center;
      gap: 3rem;
      text-align: left;
    }

    .about-cta-banner__left {
      flex: 1;
    }

    .about-cta-banner__right {
      flex: 1;
      align-items: flex-start;
    }

    .about-cta-banner__heading {
      font-size: 14px;
    }

    .about-cta-banner__text {
      font-size: 14px;
    }

    .about-cta-banner__btn {
      font-size: 10px;
      padding: 0.9rem 2.2rem;
    }
  }

  /* Desktop */
  @media (min-width: 990px) {
    .about-cta-banner-section {
      padding: 0 3rem 3rem;
    }

    .about-cta-banner {
      padding: 3.5rem 4rem;
    }

    .about-cta-banner__container {
      gap: 4rem;
    }

    .about-cta-banner__heading {
      font-size: 15px;
    }

    .about-cta-banner__text {
      font-size: 15px;
    }

    .about-cta-banner__btn {
      font-size: 11px;
      padding: 1rem 2.5rem;
    }
  }
</style>

<div class="about-cta-banner"
  {% if section.settings.background_image != blank %}
    style="background-image: url('{{ section.settings.background_image | image_url: width: 1600 }}');"
  {% endif %}
>
  <div class="about-cta-banner__container">
    <div class="about-cta-banner__left">
      {% if section.settings.heading != blank %}
        <h2 class="about-cta-banner__heading">{{ section.settings.heading }}</h2>
      {% endif %}
    </div>

    <div class="about-cta-banner__right">
      {% if section.settings.text != blank %}
        <p class="about-cta-banner__text">{{ section.settings.text }}</p>
      {% endif %}

      {% if section.settings.button_label != blank %}
        <a href="{{ section.settings.button_link }}" class="about-cta-banner__btn">
          <span class="about-cta-banner__btn-text">{{ section.settings.button_label }}</span>
        </a>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Bandeau CTA",
  "tag": "section",
  "class": "about-cta-banner-section",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Image de fond"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Titre (accroche gauche)",
      "default": "OFFREZ À VOTRE INTÉRIEUR UNE ŒUVRE PERSONNELLE, NUMÉROTÉE ET CERTIFIÉE ORIGINALE."
    },
    {
      "type": "text",
      "id": "text",
      "label": "Texte (droite, italique doré)",
      "default": "Créez dès maintenant la vôtre et donnez vie à votre thème natal."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Texte du bouton",
      "default": "CRÉER MON ŒUVRE"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Lien du bouton"
    }
  ],
  "presets": [
    {
      "name": "Bandeau CTA"
    }
  ]
}
{% endschema %}
